export interface IAppConfig {
    user: {
        name: string;
        title: string;
        description?: string;
        email: string;
        linkedin: string;
        address: string;
        addressUrl: string;
    };
}
